version: "3.9"

services:
  backend:
    build: ../../backend
    env_file:
      - .env.dev
    ports:
      - "${BACKEND_PORT}:8000"

  frontend:
    build: ../../frontend
    ports:
      - "${FRONTEND_PORT}:80"
    depends_on:
      - backend